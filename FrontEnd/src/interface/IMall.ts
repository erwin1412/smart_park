export interface IMall {
    id: string
    name: string
    district: string
    address: string
    image: string | Blob
}

export interface IMallPost {
    name: string
    district: string
    address: string
    image: string | Blob
}